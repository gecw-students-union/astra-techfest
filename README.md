# astra-techfest

This repository stores verified certificates and templates issued through GECW Cert Portal.

## Standalone Verification Web App
Open `index.html` or enable GitHub Pages to verify any certificate in this repository.

## Verification & Immutability
Every certificate in `certificates/` is an immutable git commit. Modifications or deletions are tracked via git logs.
